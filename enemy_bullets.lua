formations = {}
enemybullets = {}

function enemybullets.load() -- this loads the bullets
end

function enemybullets.fire(bool, formation, dt) -- this says whether or not the bullets are firing and what formation they should be fired in
end

function enemybullets.update(dt) -- updates bullet positions
end

function enemybullets.draw() -- draws all the above logic as bullets on-screen
end
